<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.exampole.testanko">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="AllowBackup,GoogleAppIndexingWarning">

        <activity android:name=".activity.resources.ResourcesAndDimensionsActivity"
                  android:label="Anko Commons ResourcesAndDimensions"/>

        <activity android:name=".activity.log.LogActivity"
                  android:label="Anko Commons Log"/>

        <activity android:name=".activity.dialog.DialogAndToastActivity"
                  android:label="Anko Commons DialogAndToast"/>

        <activity android:name=".activity.intent.IntentActivity"
                  android:label="Anko Commons Intent"/>

        <activity
                android:name=".activity.CoroutinesActivity"
                android:label="Anko Coroutines"/>

        <activity
                android:name=".activity.SQLiteActivity"
                android:label="Anko SQLite"/>

        <activity
                android:name=".activity.LayoutsActivity"
                android:label="Anko Layouts"/>

        <activity
                android:name=".activity.CommonsActivity"
                android:label="Anko Commons"/>

        <activity android:name=".HomeActivity"/>

        <activity
                android:name=".MainActivity"
                android:label="Anko">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>